import React from 'react';

import styles from './error.m.css';

const Error = () => {
  return (
    <div>
      Sorry problem with server or cors. Wait few second, and refresh the
      current page.
    </div>
  );
};

export default Error;
